app.controller "ConnectingWallCtrl", ($scope) ->
  $scope.showQuestionChoice = ->
    true

  $scope.showClues = ->
    false

  $scope.showAnswers = ->
    false

  $scope.showConnection = ->
    false

  $scope.showAnswer = ->
    false
